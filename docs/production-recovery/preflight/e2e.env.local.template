# -> save as  apps/web/e2e/.env.local      (already gitignored, verified)
#
# Two Supabase Auth users you create in the staging project.
# User B exists only to prove User A's data does not leak.

E2E_EMAIL=<USER_A_EMAIL>
E2E_PASSWORD=<USER_A_PASSWORD>

E2E_EMAIL_B=<USER_B_EMAIL>
E2E_PASSWORD_B=<USER_B_PASSWORD>

PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000
