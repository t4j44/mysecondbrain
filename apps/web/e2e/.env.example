# Copy to e2e/.env.local (gitignored) or export in the shell.
# Never commit real passwords.

E2E_EMAIL=
E2E_PASSWORD=

# Optional second user for RLS / cross-tenant isolation
E2E_EMAIL_B=
E2E_PASSWORD_B=

PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000
